# Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================
# AGENT INSTRUCTIONS:
# This template shows required environment variables.
#
# When generating:
# 1. Analyze package.json, config files, and codebase for actual env var usage
# 2. Check for references to process.env.* in the code
# 3. Include all API keys, database URLs, and secrets mentioned
# 4. Group related variables together with comments
# 5. Provide example values or placeholders (never real credentials)
# 6. Mark required vs optional variables
# 7. Include links to documentation for obtaining API keys
# ============================================

# Application
NODE_ENV=development # Set to 'production' for production environments
PORT=8888 # Port the Jupyter Notebook server will listen on

# Database
DATABASE_URL=postgresql://user:password@host:port/database # Connection string for the database (if used)

# Authentication & Security
AUTH_SECRET=your_secret_key_here # Secret key for signing tokens or encrypting data
PASSWORD_SALT=another_secret_string # Salt used for hashing passwords

# External APIs
API_KEY=your_api_key_here # API key for external service (e.g., image processing)
# OTHER_API_URL=https://api.example.com # Example API endpoint

# Runtime Configuration
JUPYTER_TOKEN= # Optional: Specify a token to use for Jupyter Notebook authentication. If empty, a token will be generated.
# MAX_UPLOAD_SIZE=100MB # Example: Set the maximum file upload size